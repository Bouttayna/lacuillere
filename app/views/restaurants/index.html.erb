<h1>Restaurants#Index</h1>

<ul>
  <% @restaurants.each do |restaurant|%>
  <li>
    <%=restaurant.name%>
    <%=link_to "[details]", restaurant_path(restaurant) %>
  </li>
  <%end%>
</ul>

<%=link_to "[CREATE NEW]", new_restaurant_path(@restaurant) %>

